define hostgroup {
        hostgroup_name  dhcp
        alias           DHCP servers
        members         {% for host in groups['dhcp'] %}{{host}}{% if not loop.last %},{% endif %} {% endfor %}

}

