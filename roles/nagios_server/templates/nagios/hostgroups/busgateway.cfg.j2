define hostgroup {
        hostgroup_name  bustgateway
        alias           BUSTGATEWAY servers
        members         {% for host in groups['bustgateway'] %}{{host}}{% if not loop.last %},{% endif %} {% endfor %}
}

define hostgroup {
        hostgroup_name  bustgateway-stg
        alias           STG BUSTGATEWAY servers
        members         {% for host in groups['bustgateway-stg'] %}{{host}}{% if not loop.last %},{% endif %} {% endfor %}
}

