define servicegroup {
    servicegroup_name   freemedia
    alias               FreeMedia
    members             {% for host in groups['sundries'] %}{{host}}, http_freemedia_internal  {% if not loop.last %} , {% endif %} {% endfor %}

}

