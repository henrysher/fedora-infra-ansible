{% for host in vars["phx2_external"] and hostvars[host].nagios_Check_Services['ping'] == true %}
define host {
  host_name               {{ host }}
  alias                   {{ host }}
  use                     mincheck
  address                 {{ host }}
  parents                 phx2-gw
}
{% endfor %}
