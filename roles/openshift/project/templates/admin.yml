apiVersion: v1
groupNames: []
kind: RoleBinding
metadata:
  creationTimestamp: null
  name: admins
  namespace: "{{app}}"
roleRef:
  name: admin
subjects:
- kind: User
  name: "{{fas_owner}}"
userNames:
- "{{fas_owner}}"
