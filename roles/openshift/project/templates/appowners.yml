apiVersion: v1
kind: RoleBinding
metadata:
  namespace: "{{app}}"
  name: appowners
roleRef:
  kind: Role
  name: appowner
  namespace: "{{app}}"
subjects:
{% for owner in appowners %}
- kind: User
  name: "{{owner}}"
{% endfor %}
