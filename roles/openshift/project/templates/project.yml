apiVersion: v1
kind: Project
metadata:
  annotations:
    openshift.io/description: "{{description}}"
    openshift.io/display-name: "{{app}}"
{% if env == 'staging' %}
{% else %}
  creationTimestamp: null
{% endif %}
  name: "{{app}}"
spec: {}
status: {}
