apiVersion: v1
kind: Service
metadata:
  name: caiapi
  labels:
    app: caiapi-noggin
    service: caiapi
  namespace: caiapi-noggin
spec:
  ports:
    - name: web
      port: 8080
      targetPort: 8080
  selector:
    deploymentconfig: caiapi
