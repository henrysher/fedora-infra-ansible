apiVersion: v1
kind: Service
metadata:
  name: caiapi
  labels:
    app: caiapi_noggin
    service: caiapi
  namespace: caiapi_noggin
spec:
  ports:
    - name: web
      port: 8080
      targetPort: 8080
  selector:
    deploymentconfig: caiapi
