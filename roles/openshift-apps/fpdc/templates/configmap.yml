{% macro load_file(filename) %}{% include filename %}{%- endmacro -%}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: fpdc-configmap
  labels:
    app: fpdc
data:
  config.json: |-
    {{ load_file('config.json') | indent }}
