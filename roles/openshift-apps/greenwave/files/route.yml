apiVersion: v1
kind: Route
metadata:
  name: greenwave-stg-web
  labels:
    app: greenwave
spec:
  #host: greenwave.stg.fedoraproject.org
  port:
    targetPort: web
  to:
    kind: Service
    name: greenwave-stg-web
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
