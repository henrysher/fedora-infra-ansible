apiVersion: v1
kind: Route
metadata:
  name: bodhi-web
  labels:
    app: bodhi
spec:
  host: "bodhi{{ env_suffix }}.fedoraproject.org"
  port:
    targetPort: web
  to:
    kind: Service
    name: bodhi-web
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
