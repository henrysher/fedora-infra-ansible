---
apiVersion: v1
kind: ConfigMap
metadata:
  name: bodhi-configmap
  labels:
    app: bodhi
data:
  production.ini: |-
-    {%- macro production_ini() %}    {% include "%s/bodhi2/base/templates/production.ini.j2" % roles_path %}{%- endmacro -%}
    {{ production_ini() | indent() }}
