---
- name: test playlet
  hosts: $parent_group
  tags:
  - playlet_tag_name
  vars_files:
  - vars/vars.yml

  tasks:    
  - name: my task 1
    action: copy src=files/${myfile}.txt dest=/tmp/${myfile}.txt 

